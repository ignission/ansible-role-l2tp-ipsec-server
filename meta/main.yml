galaxy_info:
  author: Shoma Nishitateno
  description: Installs and configures L2TP/IPsec server
  license: "license (BSD, MIT)"
  min_ansible_version: 1.8
  platforms:
    - name: EL
      versions:
        - all
  galaxy_tags: []

dependencies:
  - shomatan.epel
  - { shomatan.firewalld, when: ansible_connection != 'docker' }
